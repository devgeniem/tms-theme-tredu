<label for="{id|attr}" class="h6 is-block mb-3">
    {label|html}
</label>

<input id="{id|attr}" class="mb-0 mt-0 p-2 has-text-small is-family-primary has-border-2 has-border-primary" type="text"
       name="{name|html}" value="{value|html}" placeholder="{placeholder|html}">
