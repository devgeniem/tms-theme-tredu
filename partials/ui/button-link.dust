{?link.url}
    {?link.title}
        <a href="{link.url|url}" {?link.target}target="{link.target}"{/link.target}
           class="button {?icon}button--icon{/icon} {classes|attr}">
            <span class="has-width-100">
                {link.title|html}
            </span>

            {?icon}
                {>"ui/icon" class=icon_classes /}
            {/icon}
        </a>
    {/link.title}
{/link.url}
